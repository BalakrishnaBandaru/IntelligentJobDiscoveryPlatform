# Test configuration.
#
# The startup fetch must never run here. It calls the live Adzuna and Jooble
# APIs, so a @SpringBootTest context load would spend real quota on every build
# and make the suite dependent on two external services being up.
fetch.startup.enabled=false

# The scheduler is equally unwelcome in a test run, for the same reason.
fetch.schedule.enabled=false
